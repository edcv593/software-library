# 软件库 Docker 镜像构建与部署指南
# (GitHub Actions 自动构建 → Docker Hub → 极空间拉取)

## 整体流程

```
你的电脑                    GitHub                     Docker Hub          极空间 NAS
┌──────────┐   git push   ┌──────────┐   Actions    ┌──────────┐  pull  ┌──────────┐
│ app.py   │ ──────────→ │ 仓库     │ ──build──→  │ 镜像仓库  │ ────→  │ docker   │
│ Dockerfile│             │ .github/ │             │          │        │ pull+run │
│ workflows│             │ workflows│             │          │        │          │
└──────────┘             └──────────┘             └──────────┘        └──────────┘
```

**你只需要做一次：** 推代码到 GitHub → Actions 自动构建镜像 → 推送到 Docker Hub
**之后在 NAS 上：** `docker pull` + `docker run` 就行

---

## 第一步：注册账号

1. **GitHub** — https://github.com （如果已有跳过）
2. **Docker Hub** — https://hub.docker.com/signup

---

## 第二步：在 Docker Hub 创建仓库

1. 登录 https://hub.docker.com
2. 点击 **Create Repository**
3. 名称填 `software-library`
4. 可见性选 **Public**
5. 点击 **Create**

记下你的 Docker Hub 用户名，比如 `zhangsan`，完整镜像名就是 `zhangsan/software-library:latest`

---

## 第三步：在 GitHub 创建仓库

1. 登录 GitHub → New repository
2. 名称随意，比如 `software-library`
3. 选 **Private**（私有，不暴露你的代码）
4. **不要**勾选 Add README
5. 创建

---

## 第四步：配置 GitHub Secrets

GitHub Actions 需要你的 Docker Hub 账号密码才能推送镜像。

1. 进入你的 GitHub 仓库 → **Settings** → **Secrets and variables** → **Actions**
2. 点 **New repository secret**，添加两个：

| Name | Value |
|------|-------|
| `DOCKER_USERNAME` | 你的 Docker Hub 用户名 |
| `DOCKER_PASSWORD` | 你的 Docker Hub 密码或 Access Token |

> 建议用 Access Token 代替密码：Docker Hub → Account Settings → Security → New Access Token

---

## 第五步：上传代码到 GitHub

把以下文件推送到 GitHub 仓库的根目录：

```
software-library/
├── app.py                              # 主程序
├── Dockerfile                          # 镜像构建文件
├── .dockerignore
└── .github/
    └── workflows/
        └── docker-publish.yml          # GitHub Actions 配置
```

用 Git 命令推送（在你电脑上）：

```bash
# 初始化
cd "C:\Users\Administrator\WorkBuddy AI\2026-09-04-14-35-06\docker-hub"
git init
git add app.py Dockerfile .dockerignore .github/
git commit -m "Software Library Docker image"

# 关联你的仓库（替换成你的地址）
git remote add origin https://github.com/你的用户名/software-library.git
git branch -M main
git push -u origin main
```

**或者** 用 GitHub 网页端直接上传文件：
1. 仓库页面 → **uploading an existing file**
2. 拖入 app.py、Dockerfile
3. 手动创建 `.github/workflows/docker-publish.yml`（点 Create new file）

---

## 第六步：等待自动构建

推送代码后，GitHub Actions 会自动：
1. 拉取 `python:3.12-slim` 基础镜像
2. 把 `app.py` 复制进镜像
3. 构建完成
4. 推送到你的 Docker Hub 仓库

查看构建进度：
- 仓库 → **Actions** 标签页 → 看到黄色圆圈表示正在构建
- 绿色 ✅ 表示成功（约 2-3 分钟）
- 红色 ❌ 表示失败，点进去看日志

构建成功后，去 Docker Hub 你的仓库页面就能看到 `latest` 标签的镜像了。

---

## 第七步：在极空间上部署

### 7.1 拉取镜像

SSH 登录极空间后：

```bash
# 替换成你的 Docker Hub 用户名
docker pull 你的用户名/software-library:latest
```

如果拉取慢，配置加速器：
```bash
mkdir -p /etc/docker
echo '{"registry-mirrors":["https://docker.1ms.run"]}' > /etc/docker/daemon.json
systemctl restart docker
```

### 7.2 启动容器

**方式 A：docker run 命令**

```bash
docker run -d \
  --name software-library \
  --restart unless-stopped \
  -p 8899:8899 \
  -v /tmp/zfsv3/nvme17/13040705017/data/共享数据:/data:ro \
  -v /tmp/zfsv3/nvme17/13040705017/data/共享数据/logs:/app/data \
  你的用户名/software-library:latest
```

**方式 B：docker-compose（如果装了的话）**

修改 `docker-compose.yml` 中的 `image` 行，替换 `YOUR_DOCKER_USERNAME` 为你的实际用户名，然后：

```bash
cd /tmp/zfsv3/nvme17/13040705017/data/Docker/software/software-library
docker-compose up -d
```

**方式 C：极空间 Docker 管理界面**

| 配置项 | 值 |
|--------|-----|
| 镜像 | `你的用户名/software-library:latest` |
| 容器名称 | `software-library` |
| 重启策略 | `unless-stopped` |
| 端口 | `8899:8899` |
| 文件夹映射 1 | 共享数据 → `/data` (只读) |
| 文件夹映射 2 | 共享数据/logs → `/app/data` |

### 7.3 访问

```
http://极空间IP:8899
```

---

## 更新镜像

以后修改了 app.py，只需要：

```bash
git add app.py
git commit -m "update app"
git push
```

GitHub Actions 自动构建新镜像推送到 Docker Hub。

在极空间上更新：

```bash
docker pull 你的用户名/software-library:latest
docker rm -f software-library
docker run -d \
  --name software-library \
  --restart unless-stopped \
  -p 8899:8899 \
  -v /tmp/zfsv3/nvme17/13040705017/data/共享数据:/data:ro \
  -v /tmp/zfsv3/nvme17/13040705017/data/共享数据/logs:/app/data \
  你的用户名/software-library:latest
```

---

## 文件清单

| 文件 | 说明 |
|------|------|
| `app.py` | 主程序（扫描+Web服务），被 COPY 进镜像 |
| `Dockerfile` | 构建 python:3.12-slim + app.py |
| `.dockerignore` | 构建时排除的文件 |
| `.github/workflows/docker-publish.yml` | GitHub Actions 自动构建配置 |
| `docker-compose.yml` | 极空间上的 Compose 编排文件 |

---

## v6 版本功能说明

### 账号体系

- **首次访问**：页面会弹出"注册管理员"，第一个注册的账号自动成为管理员。
- 之后注册入口关闭，新账号只能由管理员在 **管理面板 → 用户管理** 中添加（可选普通用户/管理员角色）。
- **普通用户**：可以登录、浏览、下载、上传文件。
- **管理员**：额外拥有管理面板（远程下载入库、官网地址管理、用户管理、重新扫描）。

### 管理面板（仅管理员可见）

| 功能 | 说明 |
|------|------|
| 远程下载入库 | 粘贴任意 http(s) 下载地址，服务器后台把文件下载到软件库留存（最大 2GB），完成后自动刷新列表，实时显示下载进度。 |
| 官网下载地址管理 | 为每个文件设置官网地址；设置后列表卡片上会出现"官网"按钮。带文件名过滤框，支持"显示/隐藏"、"打开"、"入库"（直接把该地址的文件下载到软件库）。 |
| 用户管理 | 添加/删除账户，分配角色。 |
| 系统管理 | 手动重新扫描；系统默认每小时也会自动扫描一次。 |

### 上传文件

- 登录用户点击右上角头像 → **上传文件**，支持拖拽，最大 500MB。
- 上传的文件保存在 `/app/data/uploads`（对应 NAS 上的 `共享数据/logs/uploads`），**不需要**共享目录可写。
- 上传/远程下载完成后会自动重新扫描，新文件立即出现在列表中。

### 其他改进

- 列表直接以**文件名**显示每个文件（不再做软件名合并/识别），识别只影响分类和图标。
- 分类筛选改为**下拉框**，不再横向滑动。
- 环境变量新增 `LIB_UPLOAD_DIR`（默认 `<LIB_DATA_DIR>/uploads`），一般无需修改。
- 程序仅依赖 Python 标准库，镜像构建更快更稳。
